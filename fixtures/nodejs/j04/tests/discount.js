// This is the actual implementation file that tests import
// The model's generated test file will be written here as discount.test.js
// The implementation is available at the module level

const { calculateDiscount } = require('../fixture');
module.exports = { calculateDiscount };
